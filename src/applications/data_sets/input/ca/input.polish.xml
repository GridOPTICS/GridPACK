<?xml version="1.0" encoding="utf-8"?>
<Configuration>
  <Contingency_analysis>
    <contingencyList>contingencies_polish.xml</contingencyList>
    <groupSize>1</groupSize>
    <maxVoltage>1.1</maxVoltage>
    <minVoltage>0.9</minVoltage>
  </Contingency_analysis>
  <Powerflow>
    <networkConfiguration> Polish_model_v23.raw </networkConfiguration>
    <maxIteration>50</maxIteration>
    <tolerance>1.0e-3</tolerance>
    <!--
    <LinearSolver>
      <PETScOptions>
        -ksp_type richardson
        -pc_type lu
        -pc_factor_mat_solver_package superlu_dist
        -ksp_max_it 1
      </PETScOptions>
    </LinearSolver>
    -->
    <!--
      The superlu_dist solver (above) is preferable in most circumstances.
      However, we have found that the exception handling for this solver
      fails on some platforms, causing the contingency analysis application
      to crash. As a result, we are using the bicg solver as the default.
      If you want to try the superlu_solver, comment out the bicg solver
      below and uncomment the superlu_dist solver above.
    -->
    <LinearSolver>
      <SolutionTolerance>1.0E-15</SolutionTolerance>
      <MaxIterations>10000</MaxIterations>
      <PETScPrefix>nrs</PETScPrefix>
      <PETScOptions>
        -ksp_type bicg
        -pc_type bjacobi
        -sub_pc_type ilu -sub_pc_factor_levels 5 -sub_ksp_type preonly 
        -ksp_monitor
        -ksp_view
      </PETScOptions>
    </LinearSolver>
  </Powerflow>
</Configuration>
